这是个简单的车牌识别示例程序，采用Python、SQLite和Html，原理是Transform，采用百度飞桨PaddleOCR，识别率极高，可作为一个有趣的示例。
如要打包请使用PyInstaller，命令如下：pyinstaller --onefile --add-data "templates;templates" --add-data "models;models" --collect-all paddlex --copy-metadata ftfy --copy-metadata imagesize --copy-metadata lxml --copy-metadata opencv-contrib-python --copy-metadata openpyxl --copy-metadata premailer --copy-metadata pyclipper --copy-metadata pypdfium2 --copy-metadata scikit-learn --copy-metadata shapely --copy-metadata tokenizers --copy-metadata einops --copy-metadata jinja2 --copy-metadata regex --copy-metadata tiktoken --add-binary "E:\carscan\venv\lib\site-packages\paddle\libs;." --hidden-import paddleocr --hidden-import paddle --hidden-import cv2 --hidden-import PIL --hidden-import numpy --hidden-import flask --hidden-import sqlite3 app.py
如果需要隐藏控制台，请加上--noconsole，如下：
pyinstaller --onefile --noconsole --add-data "templates;templates" --add-data "models;models" --collect-all paddlex --copy-metadata ftfy --copy-metadata imagesize --copy-metadata lxml --copy-metadata opencv-contrib-python --copy-metadata openpyxl --copy-metadata premailer --copy-metadata pyclipper --copy-metadata pypdfium2 --copy-metadata scikit-learn --copy-metadata shapely --copy-metadata tokenizers --copy-metadata einops --copy-metadata jinja2 --copy-metadata regex --copy-metadata tiktoken --add-binary "E:\carscan\venv\lib\site-packages\paddle\libs;." --hidden-import paddleocr --hidden-import paddle --hidden-import cv2 --hidden-import PIL --hidden-import numpy --hidden-import flask --hidden-import sqlite3 app.py